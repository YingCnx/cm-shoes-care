export const openLockerSlot = async (slotNumber) => {
  console.log(`🟢 เปิดตู้ slot หมายเลข ${slotNumber}`);
  // mock command ไปยัง hardware
  return true;
};